{
  "name": "ordum-dashboard",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "build": "bun build --target=bun --minify --sourcemap --outdir=dist src/server.ts",
    "start": "cd dist && NODE_ENV=production bun run server.js",
    "validate": "tsc --noEmit",
    "format": "prettier -w ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@croct/cache": "^0.5.1",
    "@nanostores/persistent": "^1.3.3",
    "@nanostores/preact": "^1.0.0",
    "@nanostores/router": "^1.0.0",
    "nanostores": "github:Fryuni/nanostores#async-compute",
    "ohash": "^2.0.11",
    "preact": "^10.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "playwright": "^1.58.2",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "nanostores": "$nanostores"
  }
}
